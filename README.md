# 🏪 서울시 자치구별 매장 폐업률 예측

서울시 상권 공공데이터를 기반으로 자치구·업종별 **폐업 위험도를 예측**하고,
예비 창업자와 매장 운영자에게 데이터 기반 의사결정을 지원하는 서비스입니다.

> **이 저장소에 대하여**
> 이 프로젝트는 SKN 부트캠프 2차 팀 프로젝트(5인, B+)로 시작했습니다.
> 이후 **개인적으로 코드를 복기하면서, 팀 버전에서 아쉬웠던 부분(데이터 누수, 성능 부풀림, 재현성)을 개선**했습니다.
> 아래 문서는 개선된 개인 복기 버전을 기준으로 작성되었으며, 팀 원본과의 차이를 명시했습니다.

---

## 📌 한눈에 보기

| 항목 | 내용 |
|------|------|
| 문제 정의 | 서울시 자치구 × 업종 단위 **폐업 위험 이진 분류** (위험 / 정상) |
| 데이터 | 서울시 열린데이터광장 상권분석 8종 (2019 1분기 ~ 2025 2분기) |
| 관측 단위 | 분기 × 25개 자치구 × 63개 업종 |
| 최종 모델 | CatBoost (Optuna 튜닝) |
| 서비스 | Streamlit 대시보드 (예비 창업자 / 매장 운영자) |
| 실행 환경 | Python 3.13, `project_env` (venv) |

---

## 🔍 팀 원본 → 개인 복기 개선점

이번 복기의 핵심은 **"성능 수치를 높이는 것"이 아니라 "믿을 수 있는 수치를 만드는 것"** 이었습니다.

| 구분 | 팀 원본 버전 | 개인 복기 버전 (개선) |
|------|--------------|------------------------|
| 피처 | 현재 시점(t) 원본 데이터 그대로 사용 | **과거 시점(t-1, t-2) lag 피처만 사용** |
| 데이터 누수 | 예측 시점에 알 수 없는 값이 피처에 포함 | **현재 시점 데이터 전면 제외로 누수 차단** |
| 타겟 기준 | 폐업률 상위 25%(3.6%) | 폐업률 중앙값(2.3%) 기준 이진화 |
| 클래스 불균형 | SMOTE 오버샘플링 | 중앙값 기준이라 자연스럽게 균형 (오버샘플링 불필요) |
| 데이터 분할 | 랜덤 분할 (stratify) | **시계열 순서 분할** (미래 예측에 맞는 검증) |
| 그룹 통계 | 전체 데이터에서 계산 | **train에서만 계산 후 val/test에 적용** |
| 성능(CatBoost) | Acc 88% / AUC 0.95 (SMOTE 반영) | Acc 0.71 / AUC 0.80 (누수 제거 후 현실 수치) |

> 팀 버전의 높은 성능은 SMOTE와 현재 시점 데이터 사용에 상당 부분 기인했습니다.
> 누수를 제거하자 성능 지표는 낮아졌지만, **실제 예측 상황에 훨씬 가까운 정직한 성능**이 되었습니다.

---

## 📂 디렉토리 구조

```
Seoul-Store-Closure-Prediction
│
├─ data
│   ├─ raw                     # 원본 8종 CSV (서울 열린데이터광장)
│   └─ processed               # 병합/전처리 결과 (merged, train/val/test)
│
├─ notebooks                   # 분석·모델링 파이프라인 (복기 버전)
│   ├─ 00_data_merge.ipynb     # 원본 8종 → 통합 데이터(137컬럼)
│   ├─ 01_EDA.ipynb            # 탐색적 데이터 분석
│   ├─ 02_preprocessing.ipynb  # 누수 방지 피처 엔지니어링 (66피처)
│   ├─ 03_regression_trial.ipynb  # 회귀 시도 → 분류 전환 근거
│   ├─ 04_model_comparison.ipynb  # 5개 모델 비교
│   └─ 05_catboost_final.ipynb    # CatBoost + Optuna 최종 모델
│
├─ model
│   └─ catboost                # 서비스용 학습 모델(pkl) + 인코더
│
├─ streamlit                   # 웹 대시보드
│   ├─ app.py                  # 메인 (통계 요약 + 사용자 분기)
│   └─ pages
│       ├─ pre-entrepreneur.py # 예비 창업자용
│       └─ store_operator.py   # 매장 운영자용
│
├─ requirements.txt
└─ README.md
```

---

## 🗂️ 데이터

서울시 열린데이터광장 상권분석 서비스에서 제공하는 자치구 단위 8종 데이터를 사용했습니다.

| 데이터 | 내용 |
|--------|------|
| 점포 | 점포수, 개업/폐업률, 프랜차이즈 점포수 |
| 추정매출 | 요일/시간대/성별/연령대별 매출 금액·건수 |
| 상권변화지표 | 상권 변화 지표(HH/HL/LH/LL), 영업 개월 |
| 유동인구 | 시간대/요일/성별/연령대별 유동인구 |
| 상주인구 | 성별/연령대별 상주인구, 가구수 |
| 직장인구 | 성별/연령대별 직장인구 |
| 소득소비 | 월평균 소득, 항목별 지출 |
| 임대료 | 자치구별 전체 임대료 |

출처: [서울 열린데이터광장](https://data.seoul.go.kr/) · [상권 임대시세](https://golmok.seoul.go.kr/stateArea.do)

---

## ⚙️ 분석 파이프라인 (notebooks)

### 00. 데이터 병합
- 원본 8종 CSV를 자치구·업종·분기 기준으로 병합
- 임대료 데이터의 분기 표기(`2019년 1분기`) → 코드(`20191`) 변환, 키 타입 통일
- 중복 코드 컬럼 정리 → **최종 (39,975행 × 137컬럼)**

### 01. EDA
- 폐업률 분포(우측으로 치우친 long-tail), 업종별 폐업률, 상관관계 분석
- 프랜차이즈 비율과 폐업 위험의 관계 등 탐색

### 02. 전처리 & 피처 엔지니어링 (누수 방지 핵심)
- 범주형 Label Encoding (자치구 25 / 업종 63 / 상권변화 4)
- 타겟: 폐업률 **중앙값(2.3%) 이상 → 위험(1)**
- **Lag 피처 40개** (t-1, t-2) — 폐업 관련 컬럼은 제외
- **파생 피처 18개** (매출 변화율, 임대료 부담률, 경쟁 밀도 등)
- **그룹 통계 5개** (train-only 계산)
- 시계열 순서로 Train 70% / Val 10% / Test 20% 분할
- → **최종 66개 피처**

### 03. 회귀 시도 → 분류 전환 근거
- 폐업률을 연속값으로 예측하는 회귀 시도 → 3개 모델 모두 **R² ≈ 0.25**
- 예측이 평균 근처로 수렴하여 위험 구간을 못 잡음
- "몇 %인지"보다 "위험한지"가 유용 → **이진 분류로 전환**

### 04. 모델 비교
동일한 train/val/test(66피처)로 5개 모델을 공정 비교했습니다.

| 모델 | Accuracy | F1 | ROC-AUC |
|------|----------|-----|---------|
| **CatBoost** | **0.7127** | **0.6944** | **0.7955** |
| RandomForest | 0.7021 | 0.6771 | 0.7911 |
| LightGBM | 0.7013 | 0.6801 | 0.7830 |
| XGBoost | 0.7002 | 0.6808 | 0.7809 |
| TabNet | 0.7015 | 0.6723 | 0.7682 |

→ CatBoost가 세 지표 모두 1위로 최종 선택

### 05. CatBoost 최종 모델 (Optuna)
- Optuna로 검증셋 ROC-AUC 최적화 (20 trials)
- 최적 파라미터: `iterations=263, learning_rate=0.040, depth=7, l2_leaf_reg=6.17`
- **최종 테스트: Accuracy 0.7127 / F1 0.6929 / ROC-AUC 0.7995**
- Feature Importance, 혼동 행렬, ROC 곡선 시각화

---

## 🖥️ Streamlit 서비스

사용자가 자치구·업종·임대료 등을 입력하면 폐업 위험도와 상권 분석 결과를 보여줍니다.

- **메인(`app.py`)**: 서울시 요약 통계 + 사용자 유형 분기
- **예비 창업자(`pre-entrepreneur.py`)**: 자치구·업종·임대료·면적 입력 → 위험도 게이지 + 업종/자치구 비교, 트렌드, 인구·소득·소비 분석, 맞춤 솔루션(대안 지역·업종 추천)
- **매장 운영자(`store_operator.py`)**: 위 기능 + 내 매출·영업 요일/시간 입력 → 지역 평균과 비교, 영업 전략 제안

> 서비스는 실사용 시나리오(자치구·업종 선택 → 해당 상권 최신 분기 데이터로 예측)에 맞춰
> 별도의 CatBoost 모델(`model/catboost/model_and_data_encoded.pkl`)을 사용합니다.

---

## 🚀 실행 방법

```bash
# 1. 가상환경 생성 및 활성화 (Windows / Python 3.13)
python -m venv project_env
project_env\Scripts\activate

# 2. 패키지 설치
pip install -r requirements.txt

# 3. 분석 노트북 실행 (Jupyter 커널: Python (project_env))
#    notebooks/00 → 05 순서로 실행
#    00을 실행하면 data/processed/의 전처리 결과가 생성됩니다.

# 4. Streamlit 대시보드 실행
cd streamlit
streamlit run app.py
```

### 데이터 & 모델 파일 안내

이 저장소는 **분석 파이프라인의 재현성**에 초점을 맞춰, 대용량 산출물은 git에서 제외했습니다.

| 대상 | 저장소 포함 | 생성/획득 방법 |
|------|:----------:|----------------|
| `data/raw/` (원본 8종) | ✅ | 저장소에 포함 (재현 시작점) |
| `data/processed/` (전처리 결과) | ❌ | `notebooks/00`, `02` 실행 시 생성 |
| `model/catboost/*.pkl` (서비스 모델) | ❌ | 용량(75MB) 문제로 제외 — 아래 참고 |

> **서비스 데모 모델(`model_and_data_encoded.pkl`)** 은 팀 원본 버전의 CatBoost 모델로,
> 파일 크기 때문에 저장소에서 제외했습니다. (구조는 `model/catboost/model_and_data_encoded.summary.txt` 참고)
> Streamlit 데모를 로컬에서 실행하려면 이 파일이 `model/catboost/` 경로에 필요합니다.

---

## 🛠️ 기술 스택

- **언어/환경**: Python 3.13, venv
- **데이터**: pandas, numpy
- **시각화**: matplotlib, seaborn, plotly
- **ML**: scikit-learn, CatBoost, XGBoost, LightGBM
- **DL**: TabNet (pytorch-tabnet)
- **튜닝**: Optuna
- **대시보드**: Streamlit
- **직렬화**: joblib

---

## 💭 회고

### 팀 프로젝트에서
서울시 실데이터로 EDA부터 전처리, 다양한 모델 비교, Streamlit 서비스 구현까지
데이터 분석의 전 과정을 경험했습니다. (팀 B+, 5인)

### 복기하면서 배운 것
코드를 다시 들여다보며 가장 크게 느낀 점은 **"높은 성능 지표가 항상 좋은 모델을 의미하지 않는다"** 였습니다.

- 팀 버전은 현재 시점 데이터를 그대로 피처에 넣어, 예측 시점에는 알 수 없는 정보로 학습하고 있었습니다 (데이터 누수).
- SMOTE로 부풀린 성능은 실제 배포 환경을 반영하지 못했습니다.
- 이를 **lag 피처 기반 누수 제거 + 시계열 분할 + train-only 그룹 통계**로 다시 설계했고,
  성능 지표는 낮아졌지만 **재현 가능하고 정직한 파이프라인**을 얻었습니다.

낮아진 숫자를 그대로 두고 "왜 낮아졌는지"를 설명할 수 있는 것이,
숫자만 높은 모델보다 훨씬 가치 있다고 생각합니다.
